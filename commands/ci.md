---
description: "Run PRDX in CI mode (plan-only or implement from issue) — deprecated, use /prdx:auto"
argument-hint: "--issue <number> [--plan-only] [--requested-by <user>]"
---

# /prdx:ci - Deprecated Alias

> **Deprecated:** `/prdx:ci` is a backwards-compatible alias for `/prdx:auto`. Use `/prdx:auto` for new workflows.

Emit this warning, then delegate:

```
⚠️ /prdx:ci is deprecated; use /prdx:auto
```

Run `/prdx:auto` with all arguments passed to this command unchanged.
