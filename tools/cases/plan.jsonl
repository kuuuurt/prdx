{"id": "plan-0001", "input": {"request": "Add per-user rate limiting to our REST API. We've been getting hammered by a few power users who are hitting 10k+ requests per minute and it's degrading service for everyone else. Limit authenticated users to 1000 req/min and anonymous to 100 req/min. Use Redis for the counter store. Return 429 with a Retry-After header.", "projectName": "atlas-api", "stack": "Go 1.22, Chi router, PostgreSQL, Redis, deployed on Kubernetes", "codebaseSummary": "- cmd/server/main.go bootstraps the HTTP server and wires middleware\n- internal/middleware/ contains auth, logging, and CORS middleware\n- internal/handler/ has ~40 route handlers organized by resource\n- internal/store/ wraps sqlx queries; no Redis client exists yet\n- Redis is already provisioned in k8s but only used by the job queue service", "expectations": "Should address Redis client setup, middleware placement order, key schema design, and the 429 response contract; should not specify exact Redis commands or Go library implementation details."}}
{"id": "plan-0002", "input": {"request": "dark mode", "projectName": "dashboard-ui", "stack": "React 18, Tailwind CSS 3, shadcn/ui, Vite", "codebaseSummary": "- src/App.tsx is the root; no theme context exists\n- src/components/ has 80+ components all using hardcoded Tailwind color classes\n- src/styles/globals.css defines a few CSS custom properties but no color tokens\n- No design system or token library is in place\n- User preferences are not persisted anywhere currently", "expectations": "Should surface the scope challenge of retrofitting 80+ components, recommend a token/variable strategy, and flag persistence of user preference as a requirement; should not just say 'add a dark class to the body'."}}
{"id": "plan-0003", "input": {"request": "The product list screen freezes for about 2 seconds when the user scrolls past 200 items. It only happens on older Android devices (API 26-28). We're using a ListView.builder which should be lazy but something is blocking the main thread.", "projectName": "shoppr-mobile", "stack": "Flutter 3.22, Dart, Provider state management, sqflite for local cache", "codebaseSummary": "- lib/screens/product_list_screen.dart renders a ListView.builder with ~500 items loaded upfront\n- lib/models/product.dart does JSON deserialization inline during the build phase\n- lib/providers/product_provider.dart fetches and caches all products in a single call\n- No isolate usage anywhere in the codebase\n- lib/services/image_cache_service.dart performs synchronous disk reads", "expectations": "Should identify likely culprits (synchronous disk reads, inline deserialization) and recommend moving heavy work off the main isolate; should not be a generic Flutter performance checklist."}}
{"id": "plan-0004", "input": {"request": "Refactor the plugin loading system. Right now plugins are loaded by hardcoding import paths in a switch statement in core/loader.go. We want a directory-scan approach where dropping a .so file into a plugins/ folder is enough. Need to support hot reload without restarting the CLI.", "projectName": "pipe-cli", "stack": "Go 1.22, Cobra CLI framework, Go plugin package for .so loading", "codebaseSummary": "- cmd/root.go wires Cobra commands and calls core/loader.go at startup\n- core/loader.go has a 300-line switch statement mapping plugin names to hard-coded constructors\n- core/plugin_interface.go defines the Plugin interface all plugins must satisfy\n- plugins/ directory exists but is empty and gitignored\n- No file-watch mechanism exists anywhere in the codebase", "expectations": "Should address interface versioning/compatibility risks, file-watch strategy, and graceful unload semantics; should avoid specifying exact Go plugin API call sequences."}}
{"id": "plan-0005", "input": {"request": "Our nightly ETL job that syncs 50M rows from the data warehouse to our analytics Postgres instance is taking 6+ hours and we need it under 90 minutes. The business wants fresher data. We can spend on infra if needed.", "projectName": "dw-sync-pipeline", "stack": "Python 3.12, Apache Spark 3.5, PySpark, AWS S3, Redshift, PostgreSQL 15", "codebaseSummary": "- jobs/full_sync.py loads all 50M rows into a single Pandas DataFrame before writing\n- jobs/transforms/ chains ~20 transform functions applied sequentially in memory\n- utils/db.py uses psycopg2 with a connection pool size of 1\n- No partitioning or parallelism is used in the current sync job\n- Incremental sync logic exists in jobs/incremental_sync.py but covers only 5 of 40 tables", "expectations": "Should recommend partitioned parallel writes, connection pooling, and expanding incremental sync coverage; should flag the Pandas full-load as the primary bottleneck and avoid over-engineering a streaming solution for a batch problem."}}
{"id": "plan-0006", "input": {"request": "Harden our authentication flow. We've had two incidents where long-lived JWTs were used after a user was deactivated. Tokens are valid for 30 days and we have no revocation mechanism. We also store the JWT secret in a .env file that gets committed to the repo.", "projectName": "crm-backend", "stack": "Node.js 20, Express 5, PostgreSQL, jsonwebtoken, deployed on Render", "codebaseSummary": "- src/auth/jwt.js issues and verifies tokens with a hardcoded 30-day expiry\n- src/middleware/auth.js verifies the token but never checks if the user is still active\n- src/routes/auth.js handles /login and /logout; logout is client-side only\n- No refresh token flow exists\n- .env is gitignored but .env.example contains a real secret committed in a3f8c12", "expectations": "Should address short-lived access tokens with refresh token rotation, server-side revocation or token versioning, and vault/secret manager migration; should call out the leaked secret as a separate urgent remediation item distinct from the architecture work."}}
{"id": "plan-0007", "input": {"request": "We have zero visibility into what's happening in production. Add structured logging and distributed tracing so we can debug slow requests.", "projectName": "nexus-web", "stack": "Next.js 14 App Router, TypeScript, Prisma, PostgreSQL, Vercel deployment", "codebaseSummary": "- app/ uses the App Router with ~30 route segments\n- lib/db.ts exports a singleton Prisma client with no query event logging configured\n- middleware.ts handles auth redirects only\n- No logging library installed; console.log is used ad hoc throughout\n- next.config.js has no instrumentation hooks configured", "expectations": "Should cover OpenTelemetry setup for Next.js, structured JSON log format, trace propagation across server components and API routes, and Vercel's native observability integration; should not recommend a self-hosted Jaeger setup for a Vercel-deployed app."}}
{"id": "plan-0008", "input": {"request": "Add offline support to the app. Users are field technicians who often work in basements and tunnels with no connectivity. They need to fill out inspection forms, capture photos, and submit reports. When they come back online, everything should sync automatically without any action from the user.", "projectName": "fieldops-ios", "stack": "iOS 17+, Swift 5.10, SwiftUI, Core Data, URLSession", "codebaseSummary": "- Sources/Views/ has ~20 SwiftUI views including InspectionFormView and ReportDetailView\n- Sources/Models/ has Core Data entities: Inspection, Report, Photo\n- Sources/Services/APIClient.swift makes all network calls with no offline queue\n- No background task is registered in AppDelegate or Info.plist\n- Photos are uploaded immediately on capture with no local staging path", "expectations": "Should address offline queue with conflict resolution strategy, background sync registration, photo staging in Core Data or the file system, and UI state for pending/synced/failed items; should surface conflict resolution as an explicit open question rather than picking a strategy."}}
{"id": "plan-0009", "input": {"request": "Migrate the entire application from MySQL 5.7 to PostgreSQL 15. MySQL is EOL and our DBA is retiring. We have 8 years of schema migrations written in Flyway. The app uses MySQL-specific syntax in about 30 places.", "projectName": "legacy-erp", "stack": "Java 17, Spring Boot 3, Hibernate 6, Flyway, MySQL 5.7 migrating to PostgreSQL 15", "codebaseSummary": "- src/main/resources/db/migration/ has 312 Flyway SQL migration files accumulated over 8 years\n- src/main/java/com/corp/erp/repository/ has 45 JPA repositories; 8 use @Query with native SQL\n- src/main/java/com/corp/erp/config/DataSourceConfig.java is MySQL-specific\n- Several stored procedures in MySQL are called via EntityManager.createNativeQuery\n- No integration tests hit a real database; all are mocked with H2 in-memory", "expectations": "Should outline a phased migration plan covering schema conversion, native query audit, stored procedure rewrite, and dual-write validation; should flag the H2 test gap as a major risk and recommend pgloader or equivalent tooling."}}
{"id": "plan-0010", "input": {"request": "Add an autocomplete dropdown to the customer search field on the invoices page.", "projectName": "billing-portal", "stack": "Vue 3, TypeScript, Pinia, Vite, Tailwind CSS", "codebaseSummary": "- src/views/InvoicesView.vue renders the search field as a plain <input> with no interaction logic\n- src/stores/customers.ts has a Pinia store with a fetchCustomers() action that loads all customers at once\n- src/api/customers.ts wraps the REST client; no dedicated search endpoint call exists\n- The backend exposes GET /customers?q= with fuzzy match, limit, and offset params\n- No autocomplete or combobox component exists in src/components/", "expectations": "Should cover debounce strategy, accessible keyboard navigation, loading/error states, and whether to add a dedicated search action or adapt fetchCustomers; scope should stay at the UI component level without touching the backend."}}
{"id": "plan-0011", "input": {"request": "Build a pipeline that ingests clickstream events from Kafka, enriches them with user profile data from our Postgres users table, and writes enriched events to our Snowflake data warehouse. Events peak at ~5k/sec. Latency SLA is under 5 minutes end-to-end.", "projectName": "events-enrichment-pipeline", "stack": "Python 3.12, Apache Flink (PyFlink), Kafka, PostgreSQL, Snowflake Connector for Python", "codebaseSummary": "- pipelines/ directory is empty; this is greenfield within an existing data platform monorepo\n- infra/kafka.tf provisions the Kafka cluster and topic configs\n- A batch Snowflake writer exists in legacy/batch_loader.py but is not streaming-compatible\n- The users table in Postgres has ~2M rows indexed on user_id\n- The Flink cluster is provisioned in Kubernetes but no jobs are deployed", "expectations": "Should address stateful enrichment strategy (async lookup vs. broadcast state), Snowflake sink batching, checkpointing for exactly-once semantics, and backpressure handling at 5k/sec; should not include PyFlink operator code or SQL DDL."}}
{"id": "plan-0012", "input": {"request": "Break out the notification subsystem from the main Django monolith into its own service. Notifications are currently blocking HTTP request cycles and causing P95 latency spikes of 400ms.", "projectName": "platform-core", "stack": "Python 3.11, Django 4.2, Celery, Redis, PostgreSQL, Docker Compose", "codebaseSummary": "- apps/notifications/ contains models, views, Celery tasks, and email/push/SMS sending logic (~4k LOC)\n- apps/notifications/tasks.py has 12 Celery tasks called inline inside various HTTP request handlers\n- Multiple other Django apps import directly from apps.notifications.models\n- No message broker abstraction layer exists; Celery is invoked directly throughout\n- apps/notifications/tests/ achieves ~60% coverage across 3 test files", "expectations": "Should define service boundaries, an anti-corruption layer for cross-app imports, a migration path that keeps the monolith working during extraction, and an async contract (event bus vs. REST); should flag tight coupling as the primary risk and not assume a big-bang cutover."}}
{"id": "plan-0013", "input": {"request": "The app is draining the battery too fast. Users on Android 12+ are complaining it kills their phone in 4 hours during a shift. We think it's the background location polling.", "projectName": "field-tracker-android", "stack": "Android minSdk 26 targetSdk 34, Kotlin, Jetpack Compose, WorkManager, Fused Location Provider API", "codebaseSummary": "- app/src/main/java/com/co/tracker/location/LocationService.kt is a foreground Service polling GPS every 5 seconds unconditionally\n- app/src/main/java/com/co/tracker/sync/SyncWorker.kt uploads location batches every 30 seconds via WorkManager\n- The app requests ACCESS_FINE_LOCATION even for use cases where coarse location would suffice\n- No adaptive polling rate or movement-detection trigger exists\n- AndroidManifest.xml requests battery optimization exemption unconditionally", "expectations": "Should recommend adaptive polling using significant-motion or geofencing triggers, downgrading to ACCESS_COARSE_LOCATION where appropriate, less aggressive upload batching, and removing the unconditional battery optimization exemption; should not treat this as a backend or server-side problem."}}
{"id": "plan-0014", "input": {"request": "add a --dry-run flag", "projectName": "db-migrate-cli", "stack": "Rust, Clap v4, tokio async runtime, sqlx", "codebaseSummary": "- src/main.rs parses CLI args with Clap and delegates to run()\n- src/runner.rs executes migrations sequentially against the live database\n- src/plan.rs resolves which migrations are pending based on the migrations table\n- Migrations are applied in a transaction with rollback on error\n- No output formatting layer; results are printed inline in runner.rs", "expectations": "Should specify what dry-run output must show (pending migration list, SQL previews, no-write guarantee), how the flag threads through the call stack, and how to make dry-run mode unambiguous in CLI output; scope should stay minimal and not redesign the runner."}}
{"id": "plan-0015", "input": {"request": "Add a Content Security Policy header to prevent XSS. We got flagged in a pen test last month.", "projectName": "marketing-site", "stack": "SvelteKit 2, TypeScript, deployed on Cloudflare Pages", "codebaseSummary": "- src/hooks.server.ts handles server-side request hooks; no security headers are currently set\n- src/app.html contains one inline <script> block for an analytics snippet\n- Several components load third-party scripts from GTM, Intercom, and a video CDN\n- _headers file for Cloudflare Pages exists but only sets Cache-Control rules\n- No nonce generation or hash-based CSP mechanism exists", "expectations": "Should address nonce strategy for the inline script, third-party origin allowlist (GTM, Intercom, video CDN), report-only rollout mode, and whether to enforce via _headers or hooks.server.ts; should not enumerate every CSP directive value in the PRD."}}
{"id": "plan-0016", "input": {"request": "Instrument the service with OpenTelemetry so we can trace requests from the load balancer all the way through to individual database queries.", "projectName": "inventory-svc", "stack": "Go 1.22, gRPC, GORM 2, PostgreSQL, Kubernetes, Grafana Tempo as the trace backend", "codebaseSummary": "- cmd/server/main.go starts the gRPC server with no interceptor chain configured\n- internal/handler/ has ~15 gRPC service method implementations\n- internal/store/postgres.go uses GORM for all queries with no plugin callbacks\n- k8s/deployment.yaml has no OTEL_EXPORTER_* env vars set\n- Grafana Tempo is deployed in the same cluster and accepting OTLP gRPC on port 4317", "expectations": "Should cover gRPC interceptors for trace propagation, GORM plugin or callback for span-per-query, Kubernetes env var injection for the OTLP exporter, and sampling strategy; should not detail Tempo provisioning since it is already running."}}
{"id": "plan-0017", "input": {"request": "Migrate from Navigator 1.0 imperative routing to GoRouter. The current routing is a mess of nested Navigator.push calls that make deep linking impossible and back-stack management unreliable.", "projectName": "fintrack-flutter", "stack": "Flutter 3.22, Dart, GoRouter 13, Provider for state management", "codebaseSummary": "- lib/main.dart uses MaterialApp with a hand-rolled onGenerateRoute switch statement\n- lib/screens/ contains 28 screen widgets; many call Navigator.of(context).push directly\n- lib/services/deep_link_service.dart is a stub that returns false for all incoming links\n- Some screens pop with result values that calling screens depend on\n- No route constants file exists; route path strings are duplicated across the codebase", "expectations": "Should define a phased coexistence migration approach, address pop-with-result replacement semantics in GoRouter, flag screens that depend on back-stack shape as migration risk, and establish route constant conventions; should not enumerate all 28 screen routes."}}
{"id": "plan-0018", "input": {"request": "Secrets are currently passed to Airflow DAGs as plaintext environment variables in our docker-compose.yml. We need to move to a proper secrets backend before our SOC 2 audit next quarter.", "projectName": "analytics-platform", "stack": "Python 3.11, Apache Airflow 2.9, Docker Compose, AWS Secrets Manager", "codebaseSummary": "- dags/ contains 34 DAGs; most read credentials via os.environ calls directly\n- docker-compose.yml has 18 plaintext environment variable entries for database passwords and API keys\n- airflow.cfg is checked into the repository and contains several sensitive configuration values\n- A handful of DAGs use Airflow Variables (not Connections) for some config\n- AWS Secrets Manager is available and already used by other services in the account", "expectations": "Should cover Airflow's SecretsBackend interface for AWS Secrets Manager, the migration path for 34 DAGs from env vars to Connection/Variable lookups, removal of secrets from docker-compose and airflow.cfg, and an audit of which secrets need rotation; should flag the checked-in airflow.cfg as an urgent remediation item separate from the architecture work."}}
{"id": "plan-0019", "input": {"request": "Add a feature so users can re-send their welcome email from the account settings page.", "projectName": "saas-platform", "stack": "Node.js 20, Express, PostgreSQL, SendGrid, React 18", "codebaseSummary": "- server/routes/auth.js has the /register endpoint that already calls sendWelcome() on signup\n- server/services/email.js wraps SendGrid with a sendWelcome(user) function that works correctly\n- server/routes/account.js has GET and PATCH endpoints for profile management\n- client/src/pages/AccountSettings.jsx renders the settings form with no resend affordance\n- The SendGrid welcome template is functional and requires no changes", "expectations": "This request is a missing UI affordance for already-working backend capability, not a new feature — the PRD should stay tightly scoped to adding the resend endpoint and button, and must address rate limiting to prevent abuse; it should not expand into notification preferences or email template redesign."}}
{"id": "plan-0020", "input": {"request": "Our Lighthouse performance score dropped from 91 to 54 after the last release. Main thread blocking time went from 120ms to 1800ms and bundle size jumped from 380KB to 1.1MB. We need it back under 500KB and TBT under 300ms before the marketing launch next week.", "projectName": "landing-page-v2", "stack": "React 18, Vite, react-query, Framer Motion, lodash, deployed on Vercel", "codebaseSummary": "- src/App.tsx imports the entire lodash library at the top level\n- src/components/HeroSection.tsx eagerly imports all Framer Motion animation variants\n- src/pages/ has 8 route-based page components with no code splitting configured\n- vite.config.ts has no manualChunks or rollupOptions configured\n- A 2.1MB uncompressed SVG illustration was added to public/ in the last release", "expectations": "Should identify the SVG asset, lodash barrel import, and Framer Motion as the primary contributors, recommend tree-shaking, lazy route splitting, and SVG optimization, and acknowledge the timeline constraint; should not become a general React performance guide."}}
{"id": "plan-0021", "input": {"request": "Make the CLI better.", "projectName": "devtools-cli", "stack": "Python 3.12, Click, Rich, requests", "codebaseSummary": "- cli/main.py registers ~20 Click command groups\n- cli/output.py uses Rich for tables and progress bars\n- cli/api.py wraps the REST API client\n- No shell completion scripts are generated\n- GitHub issues mention slow startup, confusing error messages, and a missing --json output flag", "expectations": "This is an adversarial ambiguous request — the PRD should list explicit open questions (which commands, which user personas, what 'better' means in measurable terms) and decline to spec improvements without narrowing scope first; it may surface the GitHub issue themes as candidate requirements but must not treat them as confirmed."}}
{"id": "plan-0022", "input": {"request": "We need to build a multi-tenant billing integration with Stripe. Each tenant has its own Stripe Customer and can have multiple Subscriptions tied to different products. Admins must be able to upgrade, downgrade, and cancel subscriptions with proration handled automatically. Failed payments should trigger a dunning flow: retry on day 1, 3, and 7, then suspend the account. We are PCI-compliant via Stripe Elements only — no card data touches our servers.", "projectName": "enterprise-saas-core", "stack": "Java 17, Spring Boot 3, PostgreSQL, Stripe Java SDK 25, Kafka for inter-service events", "codebaseSummary": "- src/main/java/com/corp/saas/tenant/ manages tenant lifecycle; Tenant entity has no billing fields\n- src/main/java/com/corp/saas/billing/ package exists but is empty\n- Kafka is used for inter-service events; no BillingEvent topic exists yet\n- Stripe webhooks are not configured or validated anywhere in the codebase\n- src/main/java/com/corp/saas/admin/AdminController.java is the existing admin API surface", "expectations": "Should define the billing domain model, webhook event handling contract with idempotency strategy, dunning state machine, and admin API endpoints for plan management; should not include Stripe SDK method calls or Java code snippets."}}
{"id": "plan-0023", "input": {"request": "Add biometric authentication using Face ID. Use LocalAuthentication framework. Set LAContext, call evaluatePolicy with deviceOwnerAuthenticationWithBiometrics, handle LAError.biometryNotAvailable by falling back to passcode with deviceOwnerAuthentication, store the auth token in Keychain with kSecAttrAccessibleWhenUnlockedThisDeviceOnly, and add NSFaceIDUsageDescription to Info.plist.", "projectName": "banking-ios", "stack": "iOS 16+, Swift 5.10, SwiftUI, Combine, a Keychain wrapper library", "codebaseSummary": "- Sources/Auth/AuthViewModel.swift manages login state with @Published properties\n- Sources/Auth/KeychainService.swift wraps the Keychain with get/set/delete operations\n- Sources/Views/LoginView.swift has a PIN entry form with no biometric trigger\n- Info.plist does not contain NSFaceIDUsageDescription\n- No LocalAuthentication import exists anywhere in the codebase", "expectations": "This is an adversarial case where the request has drifted into implementation detail — the PRD should reframe inputs as requirements (biometric login, graceful fallback, secure credential storage) and strip out API-level prescription; it should surface session lifetime and re-auth trigger policy as explicit open questions."}}
{"id": "plan-0024", "input": {"request": "Migrate from the Pages Router to the App Router. We've been stuck on Next.js 12 and want to reach Next.js 14 to use React Server Components and improve TTFB.", "projectName": "ecommerce-storefront", "stack": "Next.js 12 migrating to Next.js 14, TypeScript, SWR, Styled Components, Stripe Elements", "codebaseSummary": "- pages/ has 42 route files including _app.tsx and _document.tsx\n- pages/api/ has 18 API routes used by both the frontend and external Stripe webhooks\n- Styled Components is used throughout; incompatible with React Server Components under default config\n- SWR is used for client-side data fetching across 30+ components\n- getServerSideProps or getStaticProps is used in 28 page files", "expectations": "Should outline a coexistence migration strategy running pages/ and app/ in parallel, flag Styled Components RSC incompatibility as a blocker requiring runtime config or library replacement, address API route migration including the external webhook surface, and be honest that this is a large multi-sprint migration."}}
{"id": "plan-0025", "input": {"request": "Parse incoming PDF invoices from vendors and extract line items, totals, and vendor metadata into structured database records.", "projectName": "ap-automation", "stack": "Python 3.12, FastAPI, PostgreSQL, pdfplumber, SQLAlchemy 2, Celery", "codebaseSummary": "- api/routes/invoices.py has a POST /invoices endpoint that stores raw PDF bytes and returns an ID\n- workers/invoice_worker.py is a Celery worker stub containing only a TODO comment\n- models/invoice.py defines an Invoice table with status and raw_pdf columns only; no line item schema exists\n- No extraction or parsing logic has been written; this is greenfield within an existing platform\n- tests/fixtures/ contains 12 sample vendor PDFs with varying layouts and formatting styles", "expectations": "Should address layout variance across vendors as the core challenge, recommend a confidence scoring mechanism and human-review queue for low-confidence extractions, define the structured schema for line items, and flag that pdfplumber cannot handle scanned or image-only PDFs; should not assume a single parsing strategy covers all vendors."}}
{"id":"plan-0026","input":{"request":"Our GraphQL resolver for `listOrders` takes 4-6 seconds on accounts with >500 orders. It's killing our p99. Needs to be fast.","projectName":"commerce-api","stack":"Go 1.22, gqlgen, PostgreSQL 15, pgx/v5, Redis 7","codebaseSummary":"- `graph/resolvers/order.go`: ListOrders resolver, currently loads all orders then filters in-app\n- `internal/store/order_store.go`: raw SQL queries, no pagination support\n- `internal/cache/redis.go`: generic TTL cache wrapper, used by product resolver\n- `graph/schema.graphqls`: Order type has 14 fields, 3 of which join to other tables\n- `internal/dataloader/`: DataLoader setup exists for users but not orders\n- No query-level tracing instrumentation in place","expectations":"PRD should address pagination at the DB layer, DataLoader for batch-loading joined fields, and optional caching strategy; should NOT prescribe SQL syntax or index definitions."}}
{"id":"plan-0027","input":{"request":"We need an offline-first sync engine. Users work in areas with no signal (construction sites, warehouses). Any form submission or data edit should queue locally and sync automatically when connectivity returns. Conflicts should be resolved with last-write-wins for scalar fields and union-merge for list fields. Engineers estimate 6-8 weeks. Product wants it for the Q3 launch in 11 weeks, so we have no room for scope creep. The solution must not require any schema changes on the backend.","projectName":"fieldops-mobile","stack":"Flutter 3.22, Drift (SQLite), Riverpod, Retrofit + Dio, REST backend (read-only contract from mobile's perspective)","codebaseSummary":"- `lib/data/local/app_database.dart`: Drift DB with 9 tables, auto-generated DAOs\n- `lib/data/remote/api_client.dart`: Retrofit client, all endpoints return full resource objects\n- `lib/features/`: feature-sliced structure; forms live in `inspections/`, `work_orders/`, `inventory/`\n- `lib/sync/`: directory exists but is empty — placeholder from a previous spike\n- No background isolate or WorkManager integration yet\n- Backend team confirmed: no new endpoints, no batch upsert endpoint available","expectations":"PRD should surface the missing batch-upsert endpoint as a blocking dependency and open question; should scope conflict resolution rules precisely without inventing new backend contracts."}}
{"id":"plan-0028","input":{"request":"add error boundary logging","projectName":"dashboard-ui","stack":"Svelte 5, SvelteKit 2, TypeScript, Sentry SDK 8","codebaseSummary":"- `src/routes/+error.svelte`: global SvelteKit error page, currently just renders a static message\n- `src/lib/components/ErrorBoundary.svelte`: custom wrapper using Svelte 5 snippets, swallows errors silently\n- `src/lib/sentry.ts`: Sentry.init call with dsn from env, no custom context attached\n- `src/hooks.server.ts`: handleError hook exists, logs to console only\n- Unit test coverage: ~40%, no tests for error paths","expectations":"PRD should clarify what 'logging' means (console, Sentry, or both), define what context to attach (user id, route, component tree), and call out the untested error paths as a risk."}}
{"id":"plan-0029","input":{"request":"Add a 'resend verification email' button to the signup flow","projectName":"auth-service","stack":"Node.js 20, Fastify 4, Prisma 5, PostgreSQL, SendGrid, Zod","codebaseSummary":"- `src/routes/auth/register.ts`: POST /register creates user with `verified: false`, enqueues verification email via `src/jobs/sendVerificationEmail.ts`\n- `src/routes/auth/verify.ts`: GET /verify?token=... marks user verified, token expires in 24h\n- `src/jobs/sendVerificationEmail.ts`: SendGrid template call; job occasionally fails silently (no dead-letter queue)\n- `src/middleware/requireVerified.ts`: blocks unverified users from protected routes\n- `prisma/schema.prisma`: User model has `verificationToken`, `verificationTokenExpiry`, `verified` fields\n- No resend endpoint exists; users report being stuck after token expiry","expectations":"PRD should recognize this is partly a bug report (silent job failures, no DLQ) and partly a feature request; it should separate fixing the delivery reliability problem from building the resend UI, and flag rate-limiting the resend endpoint as a security requirement."}}
{"id":"plan-0030","input":{"request":"Migrate our batch transformation pipeline from pandas to polars. We process ~200 GB of Parquet per day across 14 transformation steps. Memory pressure causes OOM kills on our 32 GB workers roughly twice a week. The migration should be incremental — we can't take the pipeline down for more than a 2-hour maintenance window. Existing unit tests must keep passing throughout.","projectName":"data-transform-pipeline","stack":"Python 3.12, pandas 2.2, polars 1.x (target), Apache Airflow 2.9, DuckDB, pyarrow","codebaseSummary":"- `pipeline/transforms/`: 14 modules, each exposes a `run(df: pd.DataFrame) -> pd.DataFrame` interface\n- `pipeline/io/parquet_reader.py`: uses `pd.read_parquet` with chunking via `chunksize` param\n- `pipeline/dag.py`: Airflow DAG wiring transforms sequentially, passes DataFrames in memory between tasks\n- `tests/transforms/`: pytest suite with 87 tests, all use `pd.testing.assert_frame_equal`\n- `pipeline/transforms/join_enrichment.py`: the heaviest transform, does 3 broadcast joins, historically the OOM trigger\n- No type annotations on transform signatures","expectations":"PRD should propose an adapter/shim strategy that lets transforms migrate one-at-a-time; should call out that test assertions using pd.testing must be updated and that the DuckDB integration needs compatibility verification."}}
